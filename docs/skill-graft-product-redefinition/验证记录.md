# Skill Graft 成品网页验证记录

## 1. 验证范围

验证日期：2026-08-26（Asia/Shanghai）。

本轮验证对象是已迁入 `panel` 的生产 UI 和 `server/product-service.mjs` 产品服务，不再把早期静态 HTML 原型的状态变化当作产品证据。主流程真实浏览器通过隔离服务 `http://127.0.0.1:18865/` 操作，隔离数据根为：

```text
C:\Users\win11\AppData\Local\Temp\skill-graft-production-final-20260826-1730\data
```

真实工作区 `E:\ozdqp-main-fix` 只做只读分析，不 attach、不融合、不接管、不写入；安装态、任务计划、端口服务、当前生产数据根和旧备份均未修改。

遵循“最小真实运行优先”：未运行全套测试、性能矩阵、故障矩阵或 Unity Test Runner；未以脚本调用产品流程替代浏览器点击。

## 2. Focused 静态与构建检查

- 根项目 `npm run build`：通过。
- panel `npm run export:web`：通过，导出 23 个静态页面（21 个 HTML、64 个导出文件）；成品入口为 `web/index.html`。
- 前端未再把 `panel/vendor/graft-glass-ui` 作为成品页面组件库或构建输入；主流程由 `ProductApp` 与 `product.css` 提供。
- 前端脚本与 `server/product-service.mjs` 的 focused `node --check` 均通过；服务以隔离数据根重启后继续完成浏览器验收。

## 3. 真实浏览器路径 A：首次分析到中心库 v1

已通过真实浏览器完成：

1. 为保证真正从空数据开始，另起一次性隔离实例 `http://127.0.0.1:18866/`，数据根为 `C:\Users\win11\AppData\Local\Temp\skill-graft-production-setup-final-20260826-1910\data`；完成截图后已停止该实例。主成品仍运行在 `http://127.0.0.1:18865/`。
2. 点击“选择工作区”，网页成功发起 Windows 原生文件夹选择器；由于自动化环境无法操作 PowerShell 模态窗口，使用页面内兼容路径输入选择 `E:\ozdqp-skill-hub`，继续的是同一套真实产品 API。
3. 点击“开始只读分析”，看到推荐项目体系及缓存/声明等只读证据。
4. 选择推荐体系，进入初始化预览；预览明确只写隔离中心库数据区，原工作区保持不变。
5. 确认创建中心库 v1，返回成品中心库页面。

结果：通过。页面主流程显示“选择工作区、分析、选择、应用、已连接”等用户文案，未把 `snapshot`、`claim`、`planSync`、`Runner` 等内部协议词作为默认操作。关键截图：

```text
C:\Users\win11\.codex\worktrees\ea9f\ozdqp-skill-hub\artifacts\skill-graft-production-acceptance\screenshots\02-initial-analysis-result.png
C:\Users\win11\.codex\worktrees\ea9f\ozdqp-skill-hub\artifacts\skill-graft-production-acceptance\screenshots\04-initial-v1-created.png
```

## 4. 真实浏览器路径 B：复杂工作区只读分析

`E:\ozdqp-main-fix` 是复杂验收工作树，存在用户脏改，禁止写入、清理和 attach。已通过成品网页完成只读分析，取消后未建立连接、未产生写入。

页面最终显示 9 类分析结果，已核对的主要内容包括：

- `baloot_client · UnitySkills REST`，约 71 个 Skill 与多个宿主投影；
- `baloot_client · Unity MCP CLI`，约 78 个 Skill，`.agents`、`.cursor`、`.claude` 投影归为同一后端家族并保留差异；
- `Tools\AIGameTesting\agent_skills` 独立私有体系；
- 项目规则入口和 Git 脏改证据；
- Git index 中约 571 条 skip-worktree 且物理缺失的休眠记录；
- `skills-lock.json` 中声明未落盘的 `game-performance-profiler` 与 `markdown-tables`；
- 聚合展示的缓存/备份证据，未将其冒充活跃体系。

根 `.agents`、锁文件缺失条目和缓存关系均以本次只读现场为准；历史线索不能覆盖实时分析结果。关键截图：

```text
C:\Users\win11\.codex\worktrees\ea9f\ozdqp-skill-hub\artifacts\skill-graft-production-acceptance\screenshots\06-main-fix-readonly-analysis.png
```

## 5. 正常 AI 聊天

已通过成品网页打开 AI 助手，发送：

```text
请用一句话说明中心库的作用，不要修改任何文件。
```

页面实际显示用户消息和 AI 回答正文：`中心库用于统一存放、管理和分发各类技能及其相关资源，作为 Skill Graft 的共享基础。` 技术状态/事件仍位于可展开详情，不替代聊天正文。关键截图：

```text
C:\Users\win11\.codex\worktrees\ea9f\ozdqp-skill-hub\artifacts\skill-graft-production-acceptance\screenshots\10-ai-chat-answer.png
```

结果：通过一次无写入正常聊天点检。AI 通过聊天发起文件修改、工具执行和完整会话恢复尚未验证。

## 6. 融合、更新、接管与回滚状态

以下流程均通过隔离数据根中的真实网页点击完成；任何写入都没有触碰 `E:\ozdqp-main-fix`、安装态或旧备份：

| 流程 | 安全验证目标 | 当前状态 |
|---|---|---|
| 融合进中心库 | incoming fixture 比较页只显示 `skills/ozdqp-development/SKILL.md` 的 `+2/-0`；成果页直接 textarea 编辑、滚动、选择和恢复；确认后生成 v2，来源工作区写入 0 | 通过 |
| 已连接工作区更新 | fixture 再新增 1 行后首页显示 1 项更新；步骤 1 显示 1 个 Skill/1 个文件；步骤 2 显示 GitHub 风格 `+1/-0` 和统一 AI 框；真实 AI 处理成功后最终页只有第 9 行绿色 | 通过；最后取消，未生成 v3 |
| 使用中心库接管 | controlled takeover fixture 完成网页预览、应用和保护边界展示 | 通过 |
| 工作区回滚 | 从接管保护点网页回滚；回读共 17 个文件，仅原 `ozdqp-ui-development` 目录保留；额外文件 0、空目录 0，根级 `skills` 和规则文件均不存在 | 通过 |
| 中心库历史回滚 | v2 已进入历史，版本说明正确；历史页面可查看版本关系 | 通过点检；未把历史回滚另记为生产工作区写入 |

可写验收 fixture（只允许写临时目录）：

```text
C:\Users\win11\AppData\Local\Temp\skill-graft-production-acceptance-20260826-1645\incoming-workspace
C:\Users\win11\AppData\Local\Temp\skill-graft-production-acceptance-20260826-1645\takeover-workspace
```

## 7. 真实工作区最终只读复核

对 `E:\ozdqp-main-fix` 保存以下摘要并与开始前比较：

- 分支 `main`，HEAD `c992cc988614aaa5f2811c28aa090496cb936d68`，相对 `origin/main` ahead 0 / behind 92；
- 10 个 modified、12 个 untracked；
- `.git\index` SHA-256 为 `2a5f2d47184060dbab53778f5ecf37156c4de5b293f2db02bf8efce476538877`；
- skip-worktree 记录仍为 571；
- 安装根、数据根和 `C:\Users\win11\AppData\Local\skill-graft-data-backup-20260825-212458` 只做存在性/顶层属性读取。

任何 status、index 哈希或 mtime 变化都属于越界：立即停止，不得 reset、checkout、clean、restore、attach 或重试写入。最终摘要证据位置：

```text
C:\Users\win11\.codex\worktrees\ea9f\ozdqp-skill-hub\artifacts\skill-graft-production-acceptance\08-final-readonly-check.txt
```

## 8. 证据截图

截图目录：

```text
C:\Users\win11\.codex\worktrees\ea9f\ozdqp-skill-hub\artifacts\skill-graft-production-acceptance\screenshots
```

主要证据包括：`01`–`04`（首次选择、分析、预览和 v1）、`05`（首页）、`06`（main-fix 只读分析）、`07`（融合文件差异）、`08`（可编辑融合成果）、`10-version-history-v2`、`10-ai-chat-answer`，以及 `11`–`17`（更新概览、AI 处理、最终成果、接管预览/应用/回滚）。最终交付以该目录的实际 PNG 文件名为准，不把缺失截图当作证据。

## 9. 未验证边界

- Windows 原生文件夹选择器的 PowerShell 模态窗口完整自动化；当前只确认网页可发起，后续浏览器链路使用兼容路径输入。
- `E:\ozdqp-main-fix` 的任何写入、attach、融合、接管和回滚；该路径永久视为只读样本。
- 真实用户工作区而非隔离 fixture 的融合、替换、接管和失败恢复；本轮只验证了受控临时 fixture。
- 全量语义归并、所有版本差异和三方冲突边界；分析结果中的家族关系仍须允许用户查看证据和改判。
- 生产安装、重装、任务计划、现有端口服务和安装态数据/旧备份行为。
- 私有 Skill 的生产级上传、删除、覆盖防护；未经明确选择不得把这些动作当作成功路径。
- AI 通过正常聊天直接修改文件、调用任意工具及完整会话持久化/恢复；本轮仅验证了无写入正常聊天和更新流程中的一次真实 AI 处理。

本记录的“通过”只代表对应的真实浏览器点击和隔离数据根证据；不能用导出网页可打开、fixture 存在或后台 API 可见来推断未验证流程已完成。
