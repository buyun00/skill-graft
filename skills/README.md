# skills/

This directory is the **local** skill corpus (resident / inbox / adopted).  
It is gitignored on purpose: project details stay on the machine that owns the hub.

Expected layout (not in git):

```text
skills/
  ozdqp-…/           resident skills
  inbox/             official updates waiting for a decision
  adopted/           skills taken from inbox
```

The published repo is only the graft runtime: CLI, adapters, overlay, panel.
