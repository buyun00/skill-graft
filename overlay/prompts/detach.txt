你在中心仓 {{HUB}} 工作，目标游戏 worktree 是 {{WORKTREE}}（已通过 --add-dir 可写）。
用户从 CLI（`sg detach`）确认了「拆掉本地 Skill 挂接、Restore 官方树」。用户意图：{{INTENT}}
不要把入队当成完成。不要再问一次确认。
固定步骤：
1. 侦察：该树是否在 overlay/attached-worktrees.txt、3 个常驻 Skill / AGENTS.override.md / overlay 是否指向 hub、官方树是否已不在磁盘、游戏仓是否有未提交业务改动。
2. 把侦察结论写进日志，然后立刻执行（不要改游戏仓业务代码）：
   拆掉指向 hub 的 3 Skill Junction、AGENTS.override.md 链接、`.codex/local-overlay` Junction（只拆链接，不删 hub 目标内容）。
   overlay/manage-skill-visibility.ps1 -Workspace '{{WORKTREE}}' -Mode Restore
   从 overlay/attached-worktrees.txt 去掉该路径。
3. 验收：官方 Skill 树回到磁盘；3 Skill 不再指向 hub；list 意义上 attached=false；未提交的业务改动还在。
4. 写入 skill-review/history，只在中心仓提交这些记录（不要提交游戏仓）。
停手条件（只有这些）：Restore 脚本失败；拆链接会删掉用户未提交的业务文件；目标树不是当前这条 worktree。
不是停手：游戏仓有未提交的业务改动（保留它们）；官方树与 hub 3 Skill 内容不同（Restore 就是为了把官方树放回来）。
禁止：-Force；-PromoteFromWorktree；改游戏仓业务代码；处理其他 worktree。
最后写一行「验收摘要：」并列出 attached / overrideLinked / officialPresent 的预期值。
