你在 Skill Graft 数据根 {{HUB}} 工作，目标游戏 worktree 是 {{WORKTREE}}（已通过 --add-dir 供只读侦察）。
用户已从 CLI 或管理面板确认「改用本地 Skill」。attach 会话 ID 是 {{SESSION_ID}}。用户意图：{{INTENT}}
不要再询问确认，也不要自行判断认树、claim、内容冲突或写盘顺序；这些判断只由共享 Core 与 Application 完成。

本次运行仍处于 attach session 的 running 阶段，不能提前 claim 或物化。唯一允许的写入口是生成当前不可变库快照：
sg snapshot create --request-id "attach-snapshot-{{SESSION_ID}}" --contract-v1

若命令失败，原样报告稳定 error code 并停手。不得运行 claim、plan-sync、sync、migrate-legacy、apply-legacy-attach，不得用文件命令、Git 命令或旧 PowerShell 脚本绕过。
命令成功后只输出一个简短的 `ATTACH_READY_V1` 交接摘要，包含：
- `sessionId={{SESSION_ID}}`
- 返回的完整 `snapshotId`
- `selectedSkills=[...]`：只从本次返回的 `snapshot.files` 推导，收集 `skills/<name>/SKILL.md` 与 `skills/adopted/<name>/SKILL.md` 中的 `<name>`，去重并按 UTF-8 字节序排列；没有任何 Skill 时必须是 `[]`，不得使用固定名称或加入快照之外的 Skill

受信 CLI/daemon 只能在本进程 exit 0、session 已成为 `awaiting` 后使用上述值依次调用 `claim`、`plan-sync` 和 `sync`，且最终 `sync` 必须继续传入同一会话 ID：`--session-id "{{SESSION_ID}}"`。若计划指出旧链接，则改走显式 `migrate-legacy --dry-run/--commit`。只有最终 pin 的 `materializedSnapshot` 与 marker 同时匹配、session 获得 completion proof 后才算完成；任一步失败都保持 awaiting/未物化，绝不能手写 history、伪造 completed、提交中心仓或修改业务代码。
